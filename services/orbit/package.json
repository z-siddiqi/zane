{
  "name": "orbit",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "node ./scripts/require-env.mjs && bunx wrangler deploy",
    "types": "bunx wrangler types",
    "check-env": "node ./scripts/require-env.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250909.0",
    "wrangler": "^3.100.0"
  },
  "dependencies": {
    "@pushforge/builder": "^1.1.0"
  }
}
