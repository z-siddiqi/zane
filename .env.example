# Port for the local WebSocket server (default: 8788)
ANCHOR_PORT=8788

# Orbit relay URL. Leave empty to run anchor in local-only mode.
# Hosted: wss://orbit.yrvgilpord.workers.dev/ws/anchor
ANCHOR_ORBIT_URL=

# Auth service URL (for device login flow)
# Hosted: https://zane.yrvgilpord.workers.dev
AUTH_URL=

# JWT token lifetime in seconds (default: 300)
ANCHOR_JWT_TTL_SEC=300

# Working directory for codex app-server (default: current directory)
ANCHOR_APP_CWD=

# Credentials file location (default: ~/.zane/credentials.json)
# ZANE_CREDENTIALS_FILE=
